@model IEnumerable<Info_Net.Models.Package>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombrePack)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Banda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombrePack)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Banda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Package_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Package_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Package_id })
        </td>
    </tr>
}

</table>
